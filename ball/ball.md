# ball.md

This module provides logic for detecting the bowling ball from different perspectives.

- `ball.py`: Shared logic for ball detection (behind, side, lane).
- Use this as a base for behind, side, and lane modules.
