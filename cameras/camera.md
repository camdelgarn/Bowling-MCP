# camera.md

This module provides generic camera and video stream handling.

- `camera.py`: Base classes and utilities for camera operations.
- Extend this for specific camera types (see gopro and brio folders).
